`discover` runs 31 tests (one of the 32 `def test_` is likely a helper or skipped/nested). The user-stated "31 tests pass" matches the actual runner. I have everything I need.

{"status":"ok","summary":"Acceptance criteria written.","data":{"acceptance_criteria":[{"id":"AC-001","text":"Running `python3 -m phase1_decomposition decompose --repo <repo> --out <out>` exits 0 and the bundle root `<out>` contains the file ARTIFACT_GUIDE.md and the directories inventory/, symbols/, rag/, static/, queries/, contracts/, tests/, and derived/.","required":true,"verification_hint":"integration_test"},{"id":"AC-002","text":"After decompose, all of these required artifact files are present and non-empty (or valid-empty per schema): inventory/files.jsonl, inventory/git-tracked-files.txt, inventory/source-coverage.json, symbols/symbols.jsonl, symbols/imports.jsonl, symbols/occurrences.jsonl, symbols/tags.jsonl, rag/spans.jsonl, rag/chunks.jsonl, rag/bm25.sqlite, rag/rg-results.jsonl, static/nodes.jsonl, static/edges.jsonl, contracts/openapi.json, contracts/contract-sources.md, tests/pytest-collect.txt, tests/test-files.jsonl, derived/repo-summary.md, derived/artifact-index.md.","required":true,"verification_hint":"integration_test"},{"id":"AC-003","text":"Decompose is byte-deterministic: running decompose twice on the same repo into two separate output directories yields byte-identical contents for every emitted artifact file (compared via per-file sha256), confirming stable ordering, sorted JSON keys, and stable hashes.","required":true,"verification_hint":"integration_test"},{"id":"AC-004","text":"Each row in inventory/files.jsonl parses as JSON and contains the keys path, language, category, size_bytes, line_count, sha256, and git_tracked; category is one of source, test, docs, config, deployment, vendor, generated.","required":true,"verification_hint":"unit_test"},{"id":"AC-005","text":"Each row in symbols/symbols.jsonl carries symbol_id, name, kind, path, range (with start_line and end_line), and signature, and symbol_id matches the SCIP-flavored grammar `python <dotted.module>/<descriptor>` (class `Name#`, function/method `name().`, module-term `name.`, module itself trailing `/`).","required":true,"verification_hint":"unit_test"},{"id":"AC-006","text":"static/nodes.jsonl uses only node types from {Repository, File, Module, Class, Function, Method, Import, Dependency, Test, ConfigFile, DocSection} and static/edges.jsonl uses only edge types from {CONTAINS, IMPORTS, CALLS_APPROX, INHERITS, DECORATED_BY, MENTIONS, TESTS_APPROX}.","required":true,"verification_hint":"unit_test"},{"id":"AC-007","text":"Running `python3 -m phase1_decomposition condense --in <bundle>` exits 0 and writes all five Step-2 condensates into <bundle>/derived/: planning-symbols.md, planning-graph.md, planning-runtime-surfaces.md, planning-tests.md, and planning-gaps.md.","required":true,"verification_hint":"integration_test"},{"id":"AC-008","text":"Running `python3 -m phase1_decomposition digest --in <bundle>` exits 0 and writes <bundle>/derived/planning-digest.md.","required":true,"verification_hint":"integration_test"},{"id":"AC-009","text":"When digest is run in upload-package mode, the planner-digest/ directory contains README_FOR_PLANNER.md, planning-digest.md, planning-symbols.md, planning-graph.md, planning-runtime-surfaces.md, planning-tests.md, planning-gaps.md, and upload-list.md.","required":true,"verification_hint":"integration_test"},{"id":"AC-010","text":"planning-graph.md contains node counts by type, edge counts by type, and ranked top items (e.g. top files/modules by degree), and planning-symbols.md contains symbol counts by kind plus ranked top items (e.g. top modules by symbol count); both include at least one ranked-list section.","required":true,"verification_hint":"static scan"},{"id":"AC-011","text":"planning-tests.md reports test file count and test function count, and planning-gaps.md explicitly lists skipped tools and known gaps (e.g. records when pytest/semgrep/ast-grep/faiss/ctags/grep-ast were unavailable).","required":true,"verification_hint":"static scan"},{"id":"AC-012","text":"The total token estimate of the planner-facing upload set (all planner-digest/ files, or the derived/planning-*.md set in non-upload mode) is at or under the configured budget (--budget-tokens, default 250000) using the ~4-chars-per-token estimate; the digest run records the computed estimate.","required":true,"verification_hint":"integration_test"},{"id":"AC-013","text":"The digest/upload package excludes the raw large artifacts: none of symbols/symbols.jsonl, static/nodes.jsonl, static/edges.jsonl, rag/chunks.jsonl, rag/spans.jsonl, or rag/bm25.sqlite is copied or inlined into planner-digest/ or planning-digest.md (verified by absence of those filenames in planner-digest/ and by upload-list.md listing only digest/markdown files).","required":true,"verification_hint":"integration_test"},{"id":"AC-014","text":"When the budget would be exceeded, trimming removes representative examples first and never removes counts, coverage warnings, or skipped-tool/gap notes (a forced-over-budget fixture still retains all count/coverage/gap sections after trimming).","required":true,"verification_hint":"unit_test"},{"id":"AC-015","text":"Graceful degradation: with pytest, semgrep, ast-grep, faiss, model2vec, universal-ctags, and grep-ast all absent, `decompose` still exits 0, produces the required core artifacts, and records each unavailable tool as a warning/skip in ARTIFACT_GUIDE.md and/or derived/planning-gaps.md rather than failing.","required":true,"verification_hint":"integration_test"},{"id":"AC-016","text":"contracts/ is produced by static discovery only by default; live OpenAPI extraction that imports/executes repo code occurs only when --contracts-import is passed, and contracts/contract-sources.md records which method was used.","required":true,"verification_hint":"integration_test"},{"id":"AC-017","text":"Test collection uses `pytest --collect-only` (collection only, never execution); when collection cannot run, tests/test-files.jsonl is produced from a static file scan and the fallback is recorded as a warning.","required":true,"verification_hint":"static scan"},{"id":"AC-018","text":"Invoking the CLI with an unknown or missing subcommand exits non-zero, and core/cli.py performs only argument parsing and dispatch (no analysis logic), with all real work under libs/.","required":true,"verification_hint":"integration_test"},{"id":"AC-019","text":"No LLM/network calls occur in any of decompose, condense, or digest: a static scan finds no HTTP client, LLM SDK, or socket-based outbound call in the libs implementation, and tests run with no real network.","required":true,"verification_hint":"static scan"},{"id":"AC-020","text":"The package ships runnable metadata: pyproject.toml exists declaring package phase1-decomposition (importable phase1_decomposition), requires-python >=3.11, core deps PyYAML and packaging, optional extras embeddings and grepast, and a tests/ directory exists.","required":true,"verification_hint":"manual review"},{"id":"AC-PY-001","text":"`python -m py_compile` succeeds on every generated `.py` file with zero errors.","required":true,"verification_hint":"py_compile"},{"id":"AC-PY-002","text":"Importing each module is side-effect free — the import smoke test imports every phase1_decomposition module and creates no service, client, repository, session, file, socket, app, worker, thread, or container.","required":true,"verification_hint":"import_smoke"},{"id":"AC-PY-003","text":"Unit tests pass and cover the success and failure paths of each required behavior; the suite runs via `python3 -m unittest discover -s tests` (pytest is not installed) and all 31 collected tests pass.","required":true,"verification_hint":"unit_test"},{"id":"AC-PY-004","text":"Integration tests pass at the CLI/package boundary (`python3 -m phase1_decomposition <decompose|condense|digest>`) using isolated temp-dir fixtures and injected/absent external tools.","required":true,"verification_hint":"integration_test"},{"id":"AC-PY-005","text":"No function or method signature uses a mutable or runtime-created default (`[]`, `{}`, `set()`, `dict()`, `list()`, `defaultdict(...)`, `Path(...)`, `datetime.now()`, or an object instance); optional collections use `None` plus in-body initialization or `field(default_factory=...)`.","required":true,"verification_hint":"static scan"},{"id":"AC-PY-006","text":"Module top level contains only imports, constants, type aliases, and class/function/dataclass definitions — no resource construction or work at import time.","required":true,"verification_hint":"static scan"},{"id":"AC-PY-007","text":"No hidden global state — no service locator, ambient container, dynamic registry, implicit singleton, or `globals()`/`getattr` dependency resolution; dependencies are explicit constructor/function parameters, provider outputs, or fixtures.","required":true,"verification_hint":"static scan"},{"id":"AC-PY-008","text":"Every resource-owning code path releases its resource via `with`, `contextlib`, or `try/finally`, even on error (e.g. sqlite3 FTS5 connections for rag/bm25.sqlite and all file handles).","required":true,"verification_hint":"static scan"},{"id":"AC-PY-009","text":"The project pulls in no dependency outside the approved dependency_policy: runtime imports are limited to the standard library plus PyYAML and packaging, with faiss-cpu/numpy/model2vec and grep-ast/tree-sitter/pathspec used only under the optional, gracefully-degrading extras.","required":true,"verification_hint":"static scan"}]},"decisions":[{"about":"AC-PY-003 test count","note":"User-stated and reference-verified: `python3 -m unittest discover -s tests` runs and passes 31 tests on the reference (tests/test_phase1.py defines 32 `def test_` but the discovered/run count is 31). Pinned the runner to `unittest discover` because pytest is not installed in the target environment."},{"about":"AC-013 (raw artifacts excluded)","note":"Raw-exclusion list taken verbatim from PHASE1_STEP2_STEP3_PLANNING_CONDENSATES.md 'Do not upload these raw backend artifacts': symbols/symbols.jsonl, static/nodes.jsonl, static/edges.jsonl, rag/chunks.jsonl, rag/spans.jsonl, rag/bm25.sqlite."},{"about":"AC-012/AC-014 (budget + trim order)","note":"Budget default 250000 tokens and ~4-chars/token estimate, plus the trim-examples-first / never-trim-counts-or-gaps rule, are from the spec's Token budget section; split into two criteria so an over-budget failure and a wrong-trim-order failure map to distinct repairs."},{"about":"AC-009 upload package","note":"Marked required because the digest stage's upload-package mode and its eight-file contents are an explicit spec contract; kept separate from AC-008 (plain planning-digest.md) so a missing upload package and a missing digest are distinct failures."},{"about":"AC-016/AC-017 (contracts + tests safety)","note":"Added as binary file/flag checks because the requirements name --contracts-import as a documented unsafe mode and pytest --collect-only as collection-only; each maps to a present-file/recorded-method check rather than a quality judgment."},{"about":"AC-018 core simplicity","note":"Encodes the hard architecture constraint 'core extremely simple, everything in libs' as a checkable bar (cli.py dispatch-only; logic under libs/) combined with the unknown-subcommand non-zero-exit behavior."}],"needs_input":[],"diagnostics":[]}