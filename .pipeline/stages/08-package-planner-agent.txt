I have everything needed. The reference uses a flat package layout with no `tests/__init__.py` (unittest discover at rootdir). The architecture is fully grounded across all prior stages. I'll produce the layout JSON matching the reference tree exactly.

{"status":"ok","summary":"Python package layout planned (flat package, matching the proven reference tree): phase1_decomposition/ at repo root with a thin core/ interface and all real work under libs/{lanes,digest,commands}, plus tests/ at root and pyproject.toml.","data":{"package_name":"phase1_decomposition","layout":"flat","packages":[{"path":"phase1_decomposition","layer":"package_root","purpose":"Top-level importable package and runtime entrypoint. Holds only side-effect-free constants (__version__='0.1.0', SCHEMA_VERSION='1', GENERATOR) in __init__.py and the `python3 -m phase1_decomposition` runner in __main__.py. __main__.py imports core/cli only.","allowed_dependencies":["core"]},{"path":"phase1_decomposition/core","layer":"interface","purpose":"Thin CLI interface only. core/cli.py parses argv for the decompose|condense|digest subcommands and dispatches to libs/commands/*.run; owns all user-facing argument I/O. Per the 'core extremely simple, everything in libs' constraint it contains no analysis logic. Imports libs.commands only; is never imported by any libs module.","allowed_dependencies":["application"]},{"path":"phase1_decomposition/libs","layer":"mixed_libs_root","purpose":"Namespace root for all real work. Directly holds the domain/value + ports + shared-infrastructure modules (the .py files: config, ids, paths, markdown, chunker, context, util, tools, rgpacks, pipeline) whose layer assignment is fixed in the architecture plan; subpackages lanes/, digest/, commands/ hold the deep adapters and use-cases. __init__.py is a side-effect-free marker.","allowed_dependencies":["domain","application","infrastructure"]},{"path":"phase1_decomposition/libs/lanes","layer":"infrastructure","purpose":"DEEP artifact-producer adapters for decompose (inventory, symbols, rag, static_graph, queries, contracts, tests, derived). Each exposes a narrow build(ctx,...) seam over substantial hidden implementation; reads the repo and writes bundle artifacts via Paths; consumes domain values (config/ids/chunker) and shared infra (util/tools/rgpacks). Never imports commands, core, or pipeline (no upward imports).","allowed_dependencies":["domain","infrastructure"]},{"path":"phase1_decomposition/libs/digest","layer":"infrastructure","purpose":"DEEP read-side condensate adapters for condense/digest (loader, ranking, planning_symbols, planning_graph, planning_runtime_surfaces, planning_tests, planning_gaps, planning_digest, upload_package). loader.Bundle is the read seam over an on-disk bundle; planning_*.build(bundle)->str render deterministic markdown; upload_package assembles the budget-bounded planner-digest/ set. Consumes domain values + loader.Bundle only; never imports lanes, commands, or core.","allowed_dependencies":["domain","infrastructure"]},{"path":"phase1_decomposition/libs/commands","layer":"application","purpose":"Application use-case / composition roots: decompose.run, condense.run, digest.run. Each constructs the explicit collaborators (Paths, Tools, Options, RunContext / Bundle) and composes lanes/ and digest/ adapters over domain values. Imported by core/cli; must never import core/cli or __main__ (dependency flows strictly one-way inward).","allowed_dependencies":["domain","infrastructure"]},{"path":"tests","layer":"test","purpose":"Single deterministic, isolated unittest suite (test_phase1.py) run via `python3 -m unittest discover -s tests`: byte-determinism assertions, artifact-schema/ID-grammar checks, graceful-degradation tests with all external tools absent, and budget/trim checks. Uses temp dirs and fake Tools registries; no real network, clock, or environment dependence.","allowed_dependencies":["domain","application","infrastructure","interface","package_root"]}],"support_files":[{"path":"pyproject.toml","purpose":"PEP 621 package metadata and build config: name 'phase1-decomposition', version 0.1.0, requires-python '>=3.11', build-backend setuptools.build_meta (requires setuptools>=68); dependencies PyYAML+packaging; optional-dependencies extras embeddings (faiss-cpu/numpy/model2vec) and grepast (grep-ast/tree-sitter-language-pack/pathspec); console script phase1-decompose=phase1_decomposition.core.cli:main; [tool.setuptools].packages explicitly lists the flat package and its 5 subpackages. Contents owned by the pyproject writer."},{"path":"README.md","purpose":"Readme referenced by pyproject readme field; documents the python3 -m phase1_decomposition <decompose|condense|digest> invocation. Content owned downstream."},{"path":"phase1_decomposition/__init__.py","purpose":"Minimal side-effect-free package marker holding only constants (__version__, SCHEMA_VERSION, GENERATOR) and the module docstring; no import-time work (PY-IMP-001/PY-IMP-002)."},{"path":"phase1_decomposition/__main__.py","purpose":"Runtime entrypoint enabling `python3 -m phase1_decomposition`; delegates to core/cli:main inside an `if __name__` guard. Required by the CLI invocation contract."},{"path":"phase1_decomposition/core/__init__.py","purpose":"Side-effect-free marker for the interface subpackage."},{"path":"phase1_decomposition/libs/__init__.py","purpose":"Side-effect-free marker for the libs root (reference contains only `from __future__ import annotations`)."},{"path":"phase1_decomposition/libs/lanes/__init__.py","purpose":"Side-effect-free marker for the lanes adapter subpackage."},{"path":"phase1_decomposition/libs/digest/__init__.py","purpose":"Side-effect-free marker for the digest summarizer subpackage."},{"path":"phase1_decomposition/libs/commands/__init__.py","purpose":"Side-effect-free marker for the commands use-case subpackage."}]},"decisions":[{"choice":"layout=flat (phase1_decomposition/ at repo root, NOT src/<package>/)","reason":"The agent default prefers src/, but stage 05 (python-runtime-agent) explicitly decided packaging=flat_package and stage 06 (architecture-planner-agent) fixed the module tree on a flat package; the proven reference at /Users/ankitsingh/Documents/deep-wiki/7-phase1-decomposition-2-proven-backup/ is flat (verified: phase1_decomposition/__init__.py + __main__.py at root, no src/ dir) and its pyproject [tool.setuptools].packages lists phase1_decomposition and its 5 subpackages directly. Re-rooting under src/ would change the proven import paths and the `python3 -m phase1_decomposition` + console-script entry point phase1_decomposition.core.cli:main, breaking ground-truth behavior the task says to preserve."},{"choice":"interface lives in core/ (not interface/) and use-cases in libs/commands/ (not application/)","reason":"Hard user architecture constraint 'core extremely simple, everything in libs': the thin CLI dispatcher is phase1_decomposition/core/cli.py and all application/use-case composition is phase1_decomposition/libs/commands/{decompose,condense,digest}.py. Standard layer names (interface/application/infrastructure/domain) are recorded in each package's `layer` field for PY-ARCH-001/002 checking, but the on-disk paths follow the mandated and reference-verified core/+libs/ shape."},{"choice":"domain/ports/shared-infra modules live as flat .py files directly under libs/ rather than in separate domain/ application/ports/ infrastructure/ subdirectories","reason":"The reference and architecture plan place config/ids/paths/markdown/chunker/context (domain+ports), util/tools/rgpacks (shared infra), and pipeline directly under libs/, with only lanes/, digest/, commands/ as subpackages. The benchmark depends on these exact subpackages being declared in pyproject; introducing extra physical layer directories would diverge from the proven tree and the packages table. Per-module layer/dependency-direction is still enforceable via the architecture plan's module-to-layer map; the package-planner only owns directory + __init__/pyproject markers, so individual .py placement under libs/ is left intact."},{"choice":"no tests/__init__.py","reason":"Tests run via `python3 -m unittest discover -s tests` (stage 05) with rootdir discovery; the reference tests/ has no __init__.py. Adding one is unnecessary and the agent's support-file note allows omitting it under rootdir discovery."},{"choice":"include README.md as a support file","reason":"pyproject sets readme='README.md', so the package metadata requires the file to exist for a valid build; the reference repo root contains README.md."},{"choice":"libs is recorded as a mixed_libs_root layer rather than a single pure layer","reason":"libs/ is a namespace root that physically contains modules spanning domain, ports, and shared-infrastructure layers plus three layered subpackages; collapsing it to one layer would misstate dependency allowances, so its allowed_dependencies is the union appropriate to the modules it hosts while the subpackages carry their own narrower layer assignments."}],"needs_input":[],"diagnostics":[]}