All key facts are grounded. Notable findings that shape the rules:

- `core/cli.py` imports only `libs.commands.*` (inward); no `libs/` module imports `core` — confirms the layer/dependency direction for PY-ARCH-001/002.
- `__init__.py` holds only constants (`__version__`, `SCHEMA_VERSION`, `GENERATOR`) — PY-IMP-002.
- `tools.py` wraps `subprocess.run` in a never-raising `try/except (OSError, subprocess.SubprocessError)` — narrow exceptions, grounds PY-EXC-001 and graceful degradation.
- The only real stdout/stderr writes: `util.log()` uses `print(..., file=sys.stderr)`; the other `print(` hits are subprocess source-code string literals (the `--contracts-import` child uses `importlib.util` + `print(json.dumps(doc))` inside a generated child script), not library-code stdout. This grounds PY-IO-001 and PY-SEC-001 (the contracts-import unsafe mode loads/executes repo code via `importlib`, the documented guarded boundary).
- No mutable defaults, no bare except, no `eval/exec/pickle` in the package itself.

Now I'll produce the rule-compliance JSON.

{"status":"ok","summary":"Rules selected.","data":{"required_rules":[{"id":"PY-MUT-001","title":"No mutable or runtime-created default arguments","severity":"error","rationale":"Lanes/digest builders pass collector objects, option dicts, and accumulator lists (e.g. RunContext warnings/artifact_table, ctx threading through libs/lanes/*.build, ranking/Counter helpers in libs/digest); a shared `=[]`/`={}`/`=Counter()`/`=Path(...)` default would persist across decompose/condense/digest invocations and silently break byte-determinism (AC-003). Validator: AST FunctionDef default nodes must contain no List/Dict/Set/Call/Path/datetime literal; use None sentinel or dataclasses.field(default_factory=...)."},{"id":"PY-IMP-001","title":"No import-time construction of services, clients, sessions, files, connections, or subprocesses","severity":"error","rationale":"Graceful degradation (AC-015) and import-safety require that importing phase1_decomposition (and every libs module) touches no external tool, opens no sqlite/FTS5 db, runs no shutil.which/subprocess, and reads no repo file. tools.py tool-detection, rag.py bm25.sqlite handle, and lanes' subprocess calls must run only inside build()/run(), never at module top level, so the package imports cleanly when pytest/semgrep/ast-grep/faiss/ctags/grep-ast are absent. Validator: module-level statements must not call subprocess/sqlite3.connect/open/shutil.which/importlib load or instantiate Tools/RunContext/Bundle."},{"id":"PY-IMP-002","title":"No module-level execution beyond imports, constants, type aliases, and definitions","severity":"error","rationale":"The package_root_constants layer (__init__.py) is verified to hold only __version__/SCHEMA_VERSION/GENERATOR, and config.py holds frozen category rules, query-pack tables, and node/edge vocab. Any module-level work (e.g. building rg packs, computing paths, calling token_estimate) would create hidden state and threaten determinism. Validator: top-level AST nodes restricted to Import/ImportFrom, Assign of literals/tuples/frozensets, AnnAssign aliases, ClassDef, FunctionDef, and `if __name__` guards."},{"id":"PY-ARCH-001","title":"Domain/core must not import infrastructure, interface, CLI, or framework modules","severity":"error","rationale":"The approved architecture fixes domain/value modules (libs/config, libs/ids, libs/paths, libs/markdown, libs/chunker, libs/context, libs/digest/loader) as pure and inward; they must not import libs/lanes/*, libs/digest/planning_*, libs/commands/*, libs/tools (subprocess adapter), libs/rgpacks, or core/cli. Reference confirms core is never imported by libs and ids depends only on config. Validator: import-graph check — domain modules' ImportFrom targets must stay within domain + stdlib, never lanes/commands/tools/core."},{"id":"PY-ARCH-002","title":"Application/use-case code depends on domain and ports/protocols (and infra adapters) only, never on the interface","severity":"error","rationale":"libs/commands/{decompose,condense,digest}.run are the application composition roots; they may compose lanes/digest infra and domain values but must never import core/cli or phase1_decomposition.__main__ (the interface). Reference shows the dependency flows core/cli -> libs.commands -> lanes/digest, strictly one-way. Validator: libs/commands/* imports must not reference core/* or __main__; the interface depends on application, not the reverse."},{"id":"PY-PROT-001","title":"Data-shape seams/ports must not import concrete adapters or external tools","severity":"error","rationale":"The narrow read-side seam libs/digest/loader.Bundle and the output-layout port libs/paths.Paths and the collector seam libs/context define the shapes threaded between layers; they must stay free of libs/tools (subprocess), libs/rgpacks, libs/lanes/*, and faiss/yaml/packaging adapters so condense/digest can read a bundle without importing decompose's infra. Validator: loader/paths/context ImportFrom targets restricted to stdlib + domain; no lanes/tools/rgpacks/digest-builder imports."},{"id":"PY-DI-001","title":"Replaceable dependencies (Paths, Tools, Options, RunContext, Bundle) are explicit parameters","severity":"error","rationale":"Lanes and digest builders must receive ctx/bundle/paths/tools as explicit build(ctx,...)/run(args)/build(bundle) parameters so tests can inject fakes (tmp dirs, fake Tools registry reporting tools absent) to prove graceful degradation and determinism deterministically. Validator: lane/command/digest entrypoints take their collaborators as function/constructor parameters rather than reaching for module-level singletons."},{"id":"PY-DI-002","title":"No service locator, global tool registry, ambient container, or globals()/getattr dependency lookup","severity":"error","rationale":"Tool availability (pytest/semgrep/ast-grep/faiss/ctags/grep-ast) is carried by an explicit Tools/ToolInfo registry constructed in the command run() and passed via RunContext, not a module-global cache or globals()/getattr probe; an ambient registry would defeat injection of an all-absent Tools fixture for AC-015 and make detection order nondeterministic. Validator: no module-level mutable registry/singleton, no globals()/getattr-based resolution of lanes or tools."},{"id":"PY-MUT-002","title":"No mutable class attributes except approved frozen constants/vocab","severity":"error","rationale":"context.RunContext intentionally holds mutable collectors (warnings, artifact_table) but they must be per-instance (set in __init__ / field(default_factory=...)), never class-level mutable attributes; config vocab (node/edge types, categories, query packs) must be class/module-level immutable tuples/frozensets so it is shared safely and stays deterministic. Validator: ClassDef bodies must not assign list/dict/set/Counter at class scope unless an approved immutable constant."},{"id":"PY-RES-001","title":"Files, sqlite connections, and subprocess handles released via with/try-finally/cleanup","severity":"error","rationale":"The RAG lane opens rag/bm25.sqlite (FTS5) and every lane writes JSONL/markdown artifacts; util JSONL/JSON writers and the sqlite connection must use `with open(...)` / a closed sqlite3 connection (try/finally) so partially written, non-deterministic, or locked artifacts never leak on error and re-runs stay byte-identical (AC-003). Validator: open()/sqlite3.connect() results bound in `with` or guarded by try/finally close()."},{"id":"PY-EXC-001","title":"No bare except, no except Exception: pass, no silent swallowing","severity":"error","rationale":"Graceful degradation must catch narrow, expected failures only — tools.run wraps subprocess in `except (OSError, subprocess.SubprocessError)`, and lane fallbacks (git ls-files, pytest --collect-only, semgrep/ast-grep, faiss/model2vec import, OpenAPI parse) must catch specific errors and record a warning/gap in ctx rather than swallowing or hiding the skip; a bare except would mask real defects and produce silent empty artifacts. Validator: no `except:`, no `except Exception:` with only pass; handlers name concrete exception types and record a note."},{"id":"PY-IO-001","title":"Library/domain/application code must not print to stdout or read stdin; only core/cli owns user I/O","severity":"error","rationale":"Determinism and the planner-facing contract require artifacts on disk, not stdout chatter; reference confirms libs only writes progress via util.log(..., file=sys.stderr) and the sole `print` to stdout is inside a generated subprocess child-script string (contracts-import), not host stdout. Diagnostics go to stderr (log) or returned warnings; result reporting stays in core/cli. Validator: no `print(` to default stdout and no input()/sys.stdin reads in libs/* except sys.stderr logging; stdout user I/O permitted only in core/cli + __main__."},{"id":"PY-SEC-001","title":"No eval/unsafe exec/unsafe pickle/shell-string commands; importlib code execution gated behind --contracts-import","severity":"error","rationale":"Decompose runs untrusted repo content, so the package must never eval/exec repo code or build shell-string commands (subprocess called with arg lists, never shell=True); the only code-execution path is the documented unsafe live-OpenAPI mode that uses importlib.util to load/run an app — verified in libs/lanes/contracts.py — and it must execute only when --contracts-import is explicitly passed and must record the method in contracts/contract-sources.md (AC-016). Validator: no eval/exec/pickle.load, no shell=True / string-built commands; importlib module-execution reachable only under the explicit contracts-import flag guard."},{"id":"PY-PKG-001","title":"Package ships runnable metadata (pyproject.toml) plus tests","severity":"error","rationale":"Project kind is an installable CLI package run as `python3 -m phase1_decomposition` with console-script phase1-decompose=core.cli:main; it must ship pyproject.toml (setuptools, requires-python >=3.11, PyYAML+packaging core deps, embeddings/grepast extras), __init__.py, __main__.py, and the tests/ suite. Validator: pyproject.toml present with build-system + packages table and a tests/ directory exists."},{"id":"PY-TEST-001","title":"Tests cover success and failure/degradation paths","severity":"error","rationale":"Acceptance requires both the happy decompose/condense/digest path AND failure/degradation paths: AC-015 (all external tools absent still exits 0), AC-014 (forced-over-budget trimming keeps counts/gaps), pytest-collect fallback to static scan, and non-git fallback. The proven suite (31 tests via unittest discover) exercises these. Validator: tests assert both produced-artifact success and tool-absent/over-budget failure handling."},{"id":"PY-TEST-002","title":"Tests prove dependency seams with fakes/stubs/in-memory adapters","severity":"error","rationale":"Graceful degradation and determinism can only be proven by injecting a fake Tools registry (all tools reporting absent) and fixture repos into tmp dirs through the explicit build(ctx)/run() seams (PY-DI-001), and by reading the resulting Bundle via loader without real semgrep/faiss/ctags. Validator: tests substitute Tools/Paths/Bundle seams with fixtures/fakes rather than requiring the real external CLIs."},{"id":"PY-TEST-003","title":"Tests are deterministic and isolated from real network, clock, filesystem state, and environment","severity":"error","rationale":"The headline guarantee is byte-determinism (AC-003): tests must run decompose into isolated tmp output dirs over fixed fixture repos, perform no network access, not depend on installed external tools or wall-clock/env, and compare per-file sha256 across runs. A test reading the developer's real filesystem or clock would be flaky and could mask determinism regressions. Validator: tests use tmp dirs/fixtures, no real socket/requests, no datetime.now()/random without seeding/injection, no reliance on host-installed tools."},{"id":"PY-VAL-001","title":"Validation cannot pass unless required tests and compile checks actually ran","severity":"error","rationale":"pytest is not installed in this environment; the gate must run the package's own suite via `python3 -m unittest discover -s tests` (and `python3 -m py_compile` over the package) and honestly report not_run with repair/execution tasks if either could not execute — never declare pass on an unrun suite. The external pytest --collect-only inside decompose is an artifact producer, not this package's test runner, and its absence must degrade, not block. Validator: require unittest-discover + py_compile execution evidence; otherwise status not_run with execution tasks."}]},"decisions":[{"note":"Included PY-ARCH-001/PY-ARCH-002/PY-PROT-001 because the approved architecture (stage 06) defines explicit one-way layers — interface (core/cli + __main__) -> application (libs/commands) -> infrastructure adapters (libs/lanes/*, libs/digest/planning_*, libs/tools, libs/rgpacks) over domain/value+port modules (libs/{config,ids,paths,markdown,chunker,context}, libs/digest/loader). Reference confirms core is never imported by libs and ids depends only on config, so these import-edge rules are mechanically enforceable on the import graph."},{"note":"Included PY-DI-002 and PY-MUT-002 because classes hold dependencies/state: RunContext carries a Tools registry plus mutable warnings/artifact_table collectors, and config exposes shared vocab. The rules forbid a global/ambient Tools registry and class-level mutable attributes while explicitly approving per-instance collectors and immutable config tuples/frozensets as safe shared state."},{"note":"Included PY-IO-001 with a project-specific tightening: the only sanctioned process output from libs is util.log via print(..., file=sys.stderr); host stdout/stdin user I/O is reserved for core/cli + __main__. The stdout print observed in libs/lanes/contracts.py is a literal inside a generated subprocess child-script string (not host stdout) and is therefore out of scope for the host-process stdout ban."},{"note":"Included PY-SEC-001 and tightened it to the documented unsafe boundary: the live-OpenAPI extraction uses importlib.util to load and execute repo code (libs/lanes/contracts.py) and must be reachable only behind the explicit --contracts-import flag (static discovery is the safe default, AC-016). subprocess must always be invoked with argument lists, never shell=True/string-built commands; no eval/exec/pickle.load anywhere."},{"note":"Included PY-PKG-001 because the project kind is an installable CLI package (pyproject.toml + console-script + python3 -m entrypoint), and PY-TEST-001/002/003 because requirements demand a deterministic, fixture-driven suite proving success, tool-absent degradation, over-budget trimming, and byte-determinism via injectable seams; PY-EXC-001 because narrow exception handling is the mechanism behind graceful degradation."},{"note":"Scoped PY-VAL-001 to the real environment: the gate runs `python3 -m unittest discover -s tests` (pytest is not installed) plus `python3 -m py_compile`; the external pytest --collect-only the decompose lane invokes is a degradable artifact producer, not this package's validation runner, so its absence must not block the gate but the unittest suite must actually run or be reported not_run."}],"needs_input":[],"diagnostics":[]}