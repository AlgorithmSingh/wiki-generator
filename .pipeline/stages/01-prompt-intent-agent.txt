I have enough grounding. The reference uses `requires-python = ">=3.11"`, deps `PyYAML` + `packaging`, optional `embeddings`/`grepast` extras, a thin `core/cli.py` dispatcher, all work in `libs/`, 31 unittest tests. Now I'll produce the intent JSON.

{"status":"ok","summary":"Intent extracted for a stdlib-first, deterministic, LLM-free Python CLI package `phase1_decomposition` that decomposes a Python repo into a repo-analysis artifact bundle (decompose) and condenses it into planner-facing digests (condense/digest), regenerated to preserve the proven reference implementation's behavior and output formats.","data":{"goal":"Build a Python CLI package `phase1_decomposition` exposing `python3 -m phase1_decomposition <decompose|condense|digest>`: `decompose` turns a Python repo into a deterministic, byte-reproducible, LLM-free repo-analysis artifact bundle (inventory/symbols/rag/static/queries/contracts/tests/derived + ARTIFACT_GUIDE.md); `condense` (Step 2) writes planning condensates into derived/; `digest` (Step 3) writes derived/planning-digest.md and an optional planner-digest/ upload package under a token budget. Regenerated code must preserve the proven reference's behavior, IDs, determinism rules, and exact artifact JSON schemas.","language":"python","project_kind":"cli","framework":null,"interface":"cli","python_version":">=3.11 (reference pyproject requires-python; runtime target Python 3.14, only python3 available)","dependency_policy":"allow_listed_deps","packaging_expectations":["installable package via pyproject.toml (setuptools build backend, build-system requires setuptools>=68)","package name `phase1-decomposition`, importable module `phase1_decomposition`","runnable as `python3 -m phase1_decomposition` (requires `__main__.py`)","console-script entry point `phase1-decompose = phase1_decomposition.core.cli:main` (per reference)","core dependencies: PyYAML, packaging","optional extras: `embeddings` (faiss-cpu, numpy, model2vec) and `grepast` (grep-ast, tree-sitter-language-pack, pathspec), opt-in/heavy","declare subpackages: phase1_decomposition, .core, .libs, .libs.lanes, .libs.digest, .libs.commands"],"test_expectations":["unit tests runnable via `python3 -m unittest discover -s tests` (NOT pytest, which is not installed)","preserve/regenerate the reference's 31-test suite passing (tests/test_phase1.py)","byte-level determinism assertions: identical artifact bytes across repeated runs","import-safety / graceful-degradation tests for missing external tools (pytest, semgrep, ast-grep, faiss, model2vec, universal-ctags, grep-ast)","cross-artifact ID linking and SCIP-style symbol id format tests","tests must be deterministic and isolated (no real network, no environment dependence; use temp dirs/fixtures)"],"source_text_constraints":["Authoritative spec for Step 1: /Users/ankitsingh/Documents/deep-wiki/7-phase1-decomposition-2-proven-backup/PHASE1_DECOMPOSITION_PLAN.md","Authoritative spec for Steps 2/3: /Users/ankitsingh/Documents/deep-wiki/7-phase1-decomposition-2-proven-backup/PHASE1_STEP2_STEP3_PLANNING_CONDENSATES.md","Ground-truth reference implementation (behavior/output formats to preserve): /Users/ankitsingh/Documents/deep-wiki/7-phase1-decomposition-2-proven-backup/phase1_decomposition/","Reference must be treated as authoritative for: SCIP-style symbol ids, cross-artifact id linking, determinism rules, exact artifact JSON schemas, and graceful tool degradation","Target output directory shape, artifact filenames, JSONL row schemas, node/edge types, and query packs are dictated verbatim by the plan docs (e.g. files.jsonl/symbols.jsonl/chunks.jsonl row shapes, node types Repository..DocSection, edge types CONTAINS/IMPORTS/CALLS_APPROX/INHERITS/DECORATED_BY/MENTIONS/TESTS_APPROX)","local grep-ast reference path: /Users/ankitsingh/Documents/deep-wiki/4-graph-based-grep/grep-ast"],"constraints":["ARCHITECTURE (hard, user): 'core extremely simple, everything in libs' — core/cli.py is a thin argument-parsing dispatcher only; all real work lives under libs/ (lanes/, digest/, commands/) as DEEP modules","modular architecture with DEEP modules (narrow interfaces, substantial hidden implementation)","CLI shape exactly `python3 -m phase1_decomposition <decompose|condense|digest>`","decompose flags: --repo, --out, --embeddings/--grep-ast/--semgrep/--ast-grep/--pytest-collect (auto|on|off), --contracts-import (flag), --rg-cap (int, default 80)","condense flags: --in, --budget-tokens (default 250000)","digest flags: --in, --out (default <bundle>/planner-digest), --budget-tokens (default 250000)","No LLM calls anywhere in Phase 1 (decompose/condense/digest)","Deterministic, byte-reproducible artifact output across runs (stable ordering, stable hashes via hashlib, sorted keys)","Graceful degradation: tool MUST work when external tools are absent (pytest/semgrep/ast-grep/faiss/model2vec/universal-ctags/grep-ast not installed) and record skips as warnings/gaps rather than failing","stdlib-first for implementation (ast, pathlib, os, hashlib, mimetypes, tomllib, sqlite3 FTS5, json); external CLI tools only where they are the industry-practice artifact producer","Use stdlib `ast` for Python symbol/graph extraction; SQLite FTS5 for BM25; ripgrep --json for exact-search results when available","Planner upload budget target <= 250K tokens; ~4 chars/token estimate; when over budget trim representative examples first, never counts/coverage warnings/gap notes","Do NOT make repo-decomposition.json or a custom framework-facts.json the canonical product; framework facts are query results + native contracts + planner summaries","condense/digest consume the exact Step-1 artifact formats and reuse the same package, paths, JSONL utilities, and conventions","No arbitrary server startup or app import for contracts by default; static contract discovery only unless --contracts-import is explicitly passed (documented unsafe mode)","Do not run the full test suite during decompose; pytest --collect-only only, and only if importable","Preserve approximate-edge confidence/provenance and warnings (e.g. unresolved dynamic calls)"],"forbidden_patterns":["LLM/API calls in Phase 1","import-time side effects (service/client/network/file/app construction at module top level)","mutable default arguments (=[], ={}, =set(), =dict(), =Path(...), =datetime.now())","global singleton / service locator / ambient global container / dynamic registry / implicit dependency lookup","bare except / except Exception: pass / silently swallowed errors","eval / unsafe exec / unsafe pickle loads / shell-string command construction","wildcard import","sys.path mutation","hard-coding MOONSHOT_API_KEY or any secret in files/logs/artifacts (relevant only to later Phase 2; must not leak here)","library/domain code printing or reading stdin for control flow (only the CLI/interface layer owns user I/O)","using pytest as the test runner for this package's own tests (use unittest; pytest is not installed)","non-deterministic output (unstable ordering, unsorted dict keys, wall-clock timestamps embedded in deterministic artifacts, randomness)","re-solving already-solved problems instead of preserving the proven reference (symbol id scheme, id linking, schemas, determinism, degradation)","fat core module containing real logic (violates 'core extremely simple, everything in libs')"],"deliverables":["Python package `phase1_decomposition` with thin core/cli.py + __main__.py dispatcher and all logic under libs/ (libs/lanes/{inventory,symbols,rag,static_graph,queries,contracts,tests,derived}.py; libs/digest/{loader,ranking,planning_symbols,planning_graph,planning_runtime_surfaces,planning_tests,planning_gaps,planning_digest,upload_package}.py; libs/commands/{decompose,condense,digest}.py; shared libs/{config,paths,chunker,util,tools,rgpacks,context,markdown,pipeline,ids}.py)","`decompose` command producing the full artifact bundle: ARTIFACT_GUIDE.md, inventory/, symbols/, rag/, static/, queries/, contracts/, tests/, derived/ per the plan's directory shape","`condense` command (Step 2) producing derived/planning-symbols.md, planning-graph.md, planning-runtime-surfaces.md, planning-tests.md, planning-gaps.md","`digest` command (Step 3) producing derived/planning-digest.md and the optional planner-digest/ package (README_FOR_PLANNER.md, copied condensates, upload-list.md)","pyproject.toml with deps PyYAML+packaging, optional embeddings/grepast extras, console-script entry point, and declared subpackages","tests/ runnable via `python3 -m unittest discover -s tests`, preserving the reference's passing suite (31 tests) including determinism and graceful-degradation coverage","README/ARTIFACT_GUIDE documentation of artifacts, producing tools, counts, warnings/skipped areas, and recommended planner reading order"],"uncertainty":["Whether the regenerated package must reproduce the reference byte-for-byte or only behaviorally/format-equivalent (task says 'preserve' behavior and output formats and 'byte-deterministic' — interpreted as: same logical schemas + per-run determinism, with reference output treated as ground truth)","Exact target Python version mismatch: environment is Python 3.14 but reference pyproject declares requires-python >=3.11; unclear whether to bump the floor (kept >=3.11 to match reference unless instructed)","Whether the console-script name `phase1-decompose` must be retained (reference defines it; task only mandates the `python3 -m` invocation)","Exact required test count/contents beyond '31/31 passing' — reference tests/test_phase1.py is authoritative but the new tests' exact wording is not specified","Whether `digest` should auto-run Step 2 condensation when condensates are missing (spec says 'Or digest can run both steps in sequence if the Step 2 condensates are missing' — optional behavior)"],"assumptions":["'core extremely simple, everything in libs' is satisfied by the reference layout: core/cli.py is a pure argparse dispatcher and all lanes/digest/commands live under libs/ — regenerate to that same shape.","Test runner is stdlib unittest (pytest unavailable); package self-tests must not depend on pytest even though decompose can shell out to pytest --collect-only on target repos.","'allow_listed_deps' = stdlib + PyYAML + packaging for core, with faiss-cpu/numpy/model2vec and grep-ast/tree-sitter-language-pack/pathspec as opt-in extras; all external CLI tools (git, rg, ctags, semgrep, ast-grep, pytest, grep-ast) are optional and degrade gracefully.","Determinism is implemented via sorted outputs, sorted JSON keys, stable hashing (hashlib/sha256), and no embedded wall-clock/random values in deterministic artifacts.","Token estimation uses the spec's ~4 chars/token heuristic; no tokenizer dependency.","The reference implementation's symbol-id scheme (SCIP-style, e.g. `python app.api.users/list_users().`) and cross-artifact id linking are reused verbatim rather than redesigned."]},"diagnostics":[]}