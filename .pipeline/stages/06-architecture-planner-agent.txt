I now have a complete and authoritative picture of the reference architecture. I have all the dependency edges, the composition flow, the import-time policy, and the layer mapping. I'll produce the architecture plan JSON.

The key finding: the reference maps to the agent's default layered model as follows — `core/cli` is the interface (thin dispatch), `libs/commands/*` are application use-cases/composition entrypoints, `libs/lanes/*` and `libs/digest/*` are infrastructure adapters (deep modules producing artifacts), `libs/{config,ids,chunker,markdown,rgpacks,paths,context}` are domain/value-objects + ports, `libs/{util,tools}` are infrastructure utilities, and the package `__init__`/`__main__` are constants + the runtime entrypoint. Dependency direction flows strictly inward; `core` is never imported by `libs`.

{"status":"ok","summary":"Architecture planned: a thin interface (core/cli + __main__) dispatches to three application use-case roots (libs/commands/{decompose,condense,digest}.run), which compose deep infrastructure modules (libs/lanes/* artifact producers, libs/digest/* condensate summarizers) over pure domain/value modules (libs/{config,ids,chunker,markdown,paths,context,loader.Bundle}) and shared infrastructure utilities (libs/{util,tools,rgpacks}). Dependency direction is strictly inward and one-way; core is never imported by libs; all real work lives in libs as DEEP modules behind narrow run()/build()/assemble() seams, matching the proven reference exactly.","data":{"layers":[{"name":"package_root_constants","maps_to":"domain/core","modules":["phase1_decomposition/__init__.py"],"role":"Pure module-level constants only (__version__='0.1.0', SCHEMA_VERSION='1', GENERATOR string). No logic, no I/O, no inward-of-stdlib imports. Imported by pipeline, queries lane, contracts-adjacent code, and the decompose command for provenance stamping.","reference":"phase1_decomposition/__init__.py"},{"name":"domain_values_and_ports","maps_to":"domain/core + ports/protocols","modules":["phase1_decomposition/libs/config.py","phase1_decomposition/libs/ids.py","phase1_decomposition/libs/paths.py","phase1_decomposition/libs/markdown.py","phase1_decomposition/libs/chunker.py","phase1_decomposition/libs/context.py","phase1_decomposition/libs/digest/loader.py"],"role":"Pure value objects, deterministic functions, and the data-shape seams threaded through the pipeline. config.py = frozen tuples/dicts of category rules, query-pack definitions, node/edge type vocab, id-grammar constants. ids.py = SCIP-style symbol_id / span_id / chunk_id / node_id construction (deterministic, stdlib-only, depends only on config). paths.py = Paths dataclass resolving every bundle location (the output-layout port). markdown.py = Section/md_table/top/count_by rendering value helpers (Counter-based, pure). chunker.py = deterministic span/chunk splitting (depends on config+ids+util pure helpers). context.py = RunContext/Options dataclasses + artifact_table (the mutable collector value-object threaded through lanes; holds Paths+Tools+Options but constructs neither). loader.Bundle = the read-side dataclass DTO + load_bundle() reader (the single seam between on-disk bundle and digest summarizers).","reference":"phase1_decomposition/libs/{config,ids,paths,markdown,chunker,context}.py; phase1_decomposition/libs/digest/loader.py"},{"name":"infrastructure_shared_utilities","maps_to":"infrastructure/adapters","modules":["phase1_decomposition/libs/util.py","phase1_decomposition/libs/tools.py","phase1_decomposition/libs/rgpacks.py"],"role":"Cross-cutting I/O + process adapters that lanes call. util.py = filesystem/JSON/JSONL writers, hashing (hashlib), text helpers, token_estimate, log (stderr). tools.py = external-tool detection (shutil.which + clean-subprocess import probes), the never-raising subprocess run() wrapper, ToolInfo/Tools registry, graceful-degradation notes. rgpacks.py = shared ripgrep query-pack runner returning RgData, consumed by both rag and queries lanes (depends on context+tools+util). These implement the 'reach outside the process / touch the filesystem' seams that domain/application must not perform directly.","reference":"phase1_decomposition/libs/{util,tools,rgpacks}.py"},{"name":"infrastructure_lanes","maps_to":"infrastructure/adapters","modules":["phase1_decomposition/libs/lanes/inventory.py","phase1_decomposition/libs/lanes/symbols.py","phase1_decomposition/libs/lanes/rag.py","phase1_decomposition/libs/lanes/static_graph.py","phase1_decomposition/libs/lanes/queries.py","phase1_decomposition/libs/lanes/contracts.py","phase1_decomposition/libs/lanes/tests.py","phase1_decomposition/libs/lanes/derived.py"],"role":"DEEP artifact-producer adapters — the heart of decompose. Each exposes a narrow build(ctx, ...) seam over substantial hidden implementation (AST walking, FTS5 BM25 indexing, graph construction, ripgrep/semgrep/ast-grep invocation, static OpenAPI discovery, pytest --collect-only, deterministic summary rendering). They consume domain values (config/ids/chunker) and shared infra (util/tools/rgpacks), read the repo, and write bundle artifacts via Paths. They never import commands, core, or the pipeline (no upward imports).","reference":"phase1_decomposition/libs/lanes/*.py"},{"name":"infrastructure_digest_summarizers","maps_to":"infrastructure/adapters","modules":["phase1_decomposition/libs/digest/ranking.py","phase1_decomposition/libs/digest/planning_symbols.py","phase1_decomposition/libs/digest/planning_graph.py","phase1_decomposition/libs/digest/planning_runtime_surfaces.py","phase1_decomposition/libs/digest/planning_tests.py","phase1_decomposition/libs/digest/planning_gaps.py","phase1_decomposition/libs/digest/planning_digest.py","phase1_decomposition/libs/digest/upload_package.py"],"role":"DEEP read-side condensate adapters powering condense/digest. ranking.py = shared ranking/markdown-section helpers over markdown.py. Each planning_*.build(bundle)->str transforms the loader.Bundle DTO into one deterministic markdown condensate. upload_package.assemble(...) = the budget-aware upload-package builder (copies only digest/markdown files, never raw large artifacts, trims examples-first under the token budget). They depend only inward on loader.Bundle, markdown, and util; never on commands/core/lanes.","reference":"phase1_decomposition/libs/digest/*.py"},{"name":"application_pipeline","maps_to":"application/use_cases","modules":["phase1_decomposition/libs/pipeline.py"],"role":"The decompose orchestration use-case: run(ctx) executes every lane in fixed dependency order with per-lane error isolation (a failing lane becomes a warning, not a crash), records timings, then writes run-metadata.json and ARTIFACT_GUIDE.md. It depends on the lanes package, context value-object, util, rgpacks, and package constants — but is itself invoked only by the decompose command. It constructs nothing external; it receives a fully-built RunContext.","reference":"phase1_decomposition/libs/pipeline.py"},{"name":"application_commands","maps_to":"application/use_cases (each run() is a composition root)","modules":["phase1_decomposition/libs/commands/decompose.py","phase1_decomposition/libs/commands/condense.py","phase1_decomposition/libs/commands/digest.py"],"role":"Three use-case entrypoints, each a named composition root. decompose.run(args): resolves repo/out paths, builds Paths, calls tools.detect_all(), constructs Options + RunContext, then calls pipeline.run(ctx). condense.run(args): loader.load_bundle(in_dir) then write_condensates() over the five planning_* summarizers. digest.run(args): load_bundle, (re)generate condensates via condense.write_condensates for consistency, write planning-digest.md, then upload_package.assemble() the planner-digest/ folder under budget. These are the only modules permitted to construct RunContext/Options and to call detect_all() — runtime construction lives here, never at import time.","reference":"phase1_decomposition/libs/commands/*.py"},{"name":"interface_cli","maps_to":"interface/cli_api","modules":["phase1_decomposition/core/cli.py"],"role":"Thin argument-parsing dispatcher ONLY (the 'extremely simple core'). build_parser() defines the decompose/condense/digest subparsers and flags; main(argv) parses and routes to the matching command.run(args), returning its int exit code. Owns no business logic; the single allowed import edge from core into libs is core/cli -> libs.commands.{decompose,condense,digest}. core is NEVER imported by anything under libs.","reference":"phase1_decomposition/core/cli.py"},{"name":"interface_runtime_entrypoint","maps_to":"interface/cli_api (runtime root)","modules":["phase1_decomposition/__main__.py","console_script:phase1-decompose"],"role":"Process entrypoint for `python3 -m phase1_decomposition <subcommand>` and the console-script phase1-decompose = phase1_decomposition.core.cli:main. __main__ imports core.cli.main and runs raise SystemExit(main()) only under `if __name__ == '__main__'`. The single runtime root that triggers work; nothing executes at import.","reference":"phase1_decomposition/__main__.py; pyproject [project.scripts]"},{"name":"tests","maps_to":"tests","modules":["tests/test_phase1.py"],"role":"Deterministic, isolated unittest suite (run via `python3 -m unittest discover -s tests`; pytest not installed). Builds fixture repos in tmp dirs, runs main(['decompose',...])/['condense'...]/['digest'...] or command.run directly, and asserts artifact presence, JSONL schemas, SCIP id grammar, node/edge vocab, byte-determinism (per-file sha256 across two runs), graceful degradation with all optional tools absent, and budget/trim invariants. No real network; external tools probed-and-skipped, not required.","reference":"tests/test_phase1.py (reference: 31 tests pass via unittest discover)"}],"dependency_direction":["__main__ -> core.cli -> libs.commands.{decompose,condense,digest}  (the ONLY interface->application edge; one-way)","libs.commands.decompose -> libs.context, libs.tools, libs.paths, libs.pipeline, libs.util, package.__init__(GENERATOR)","libs.commands.condense -> libs.digest.loader, libs.digest.{planning_symbols,planning_graph,planning_runtime_surfaces,planning_tests,planning_gaps}, libs.util","libs.commands.digest -> libs.commands.condense (reuses write_condensates), libs.digest.{loader,planning_digest,upload_package}, libs.util","libs.pipeline -> libs.lanes.* , libs.context, libs.rgpacks, libs.util, package.__init__(GENERATOR,SCHEMA_VERSION,__version__)","libs.lanes.* -> libs.config, libs.ids, libs.chunker, libs.context, libs.tools, libs.util, libs.rgpacks  (queries lane also -> package.__init__(SCHEMA_VERSION))","libs.rgpacks -> libs.context, libs.tools, libs.util","libs.digest.{planning_*,planning_digest} -> libs.digest.ranking, libs.digest.loader.Bundle","libs.digest.ranking -> libs.markdown","libs.digest.upload_package -> libs.util","libs.chunker -> libs.config, libs.ids, libs.util","libs.ids -> libs.config","libs.context -> libs.paths, libs.tools","libs.config / libs.paths / libs.markdown / libs.util / libs.tools -> stdlib only","INVARIANT: dependencies flow inward only — interface -> application -> infrastructure -> domain; domain/value modules (config, ids, paths, markdown, loader.Bundle) import nothing inward of stdlib","INVARIANT: nothing under libs/ imports core/ (verified in reference: zero `from ..core`/`from ...core` edges in libs)","INVARIANT: lanes never import commands/pipeline/core; digest summarizers never import lanes/commands/core (no upward edges)"],"composition_roots":[{"name":"main(argv)","location":"phase1_decomposition/core/cli.py","constructs":"argparse.ArgumentParser via build_parser(); routes to the selected command.run(args). Builds no adapters itself.","kind":"cli_dispatch"},{"name":"decompose.run(args)","location":"phase1_decomposition/libs/commands/decompose.py","constructs":"Paths(repo,out); Tools via tools.detect_all(); context.Options(...); context.RunContext(paths,tools,opts); then calls pipeline.run(ctx). This is where all runtime construction for Step 1 happens.","kind":"use_case_root"},{"name":"condense.run(args) / write_condensates(bundle, derived_dir)","location":"phase1_decomposition/libs/commands/condense.py","constructs":"Bundle via loader.load_bundle(in_dir); iterates the CONDENSATES (filename -> summarizer module) table writing each markdown file with per-summarizer error isolation.","kind":"use_case_root"},{"name":"digest.run(args)","location":"phase1_decomposition/libs/commands/digest.py","constructs":"Bundle via loader.load_bundle(in_dir); reuses condense_cmd.write_condensates for consistency; writes planning-digest.md; calls upload_package.assemble(in_dir,out_dir,budget,generated_at,repo_root).","kind":"use_case_root"},{"name":"__main__ guard / console_script entry","location":"phase1_decomposition/__main__.py and pyproject [project.scripts] phase1-decompose=phase1_decomposition.core.cli:main","constructs":"Calls main(); the only place SystemExit(main()) is raised. Runtime-only, never at import.","kind":"process_entrypoint"},{"name":"unittest fixtures","location":"tests/test_phase1.py","constructs":"Temp fixture repos, Paths/RunContext or direct main([...]) invocations, used as the test-time composition root with all optional external tools treated as absent/skippable.","kind":"test_fixture"}],"import_time_policy":{"allowed":["phase1_decomposition/__init__.py: module-level str constants only (__version__, SCHEMA_VERSION, GENERATOR)","libs/config.py: frozen literal tables (categories, query packs, node/edge type vocab, id-grammar constants) as module constants","libs/paths.py, libs/context.py, libs/tools.py(ToolInfo/Tools), libs/digest/loader.py(Bundle), libs/markdown.py(Section): @dataclass definitions only","libs/ids.py, libs/chunker.py, libs/util.py, libs/rgpacks.py, libs/markdown.py, libs/digest/ranking.py: pure function/class definitions and `from . import` statements","libs/lanes/*.py and libs/digest/planning_*.py: function definitions (build/assemble) and module-level constant tables (e.g. condensate/reading-order lists, RG pack maps) only","core/cli.py: build_parser/main function defs, the _TOGGLE tuple and DEFAULT_BUDGET_TOKENS constant, and the three `from ..libs.commands import ...` statements","libs/commands/*.py: run()/helper function defs plus the CONDENSATES (name->module) constant table","All modules: `from __future__ import annotations` and stdlib imports at top level"],"forbidden":["Constructing RunContext, Options, Paths, Tools, or Bundle at module top level (only inside command.run()/pipeline/fixtures)","Calling tools.detect_all(), tools.run(), or any subprocess at import time (detection happens inside decompose.run)","Opening/reading/writing files, creating directories (Paths.ensure), or computing hashes at import time","Running any lane build(), pipeline.run(), a summarizer build(), or upload_package.assemble() at import time","Importing optional heavy deps (faiss, model2vec, numpy, grep_ast, tree_sitter, yaml, semgrep) at module top level — they are probed in clean subprocesses / lazily, so absence degrades gracefully and import stays cheap","Calling main()/command.run() at import (only under the __main__ guard or the console-script callable)","Any `from ..core`/`from ...core` import inside libs/ (core must never be imported by libs)","Any `print()` for control flow in libs/ — diagnostics go through util.log to stderr; user-facing stdout/exit codes are owned by the CLI/commands edge","Network access, clock-as-control-flow, or randomness at import (determinism + isolation requirement)"]},"boundaries":{"domain":{"layer":"domain/core","modules":["phase1_decomposition/__init__.py","libs/config.py","libs/ids.py","libs/chunker.py","libs/markdown.py"],"may_import":["stdlib only","ids/chunker may import the sibling pure modules config and util (util is a pure-from-the-domain's-view helper of hashing/text); chunker -> config,ids,util"],"must_not_import":["any lane, any digest summarizer, commands, pipeline, core, tools, rgpacks, paths, context, or any optional third-party framework"],"notes":"Deterministic value/transform core: SCIP id grammar, chunk splitting, category/vocab tables, markdown rendering primitives. Testable with no I/O."},"application":{"layer":"application/use_cases","modules":["libs/pipeline.py","libs/commands/decompose.py","libs/commands/condense.py","libs/commands/digest.py"],"may_import":["domain values (config/ids/markdown indirectly)","ports/value DTOs (paths.Paths, context.RunContext/Options, digest.loader.Bundle)","infrastructure seams it orchestrates (libs.lanes.* via pipeline; libs.digest.* summarizers; libs.tools.detect_all; libs.util; libs.rgpacks)","package constants (GENERATOR/SCHEMA_VERSION/__version__)","digest.run may import condense (sibling use-case reuse)"],"must_not_import":["core/cli (no upward edge to interface)","heavy third-party frameworks at import time"],"notes":"Each command.run is a composition root; pipeline is the decompose orchestrator. Construction is runtime-only."},"ports":{"layer":"ports/protocols (expressed as dataclass DTOs + narrow build/run/assemble seams, not typing.Protocol in this reference)","modules":["libs/paths.py (Paths: output-location port)","libs/context.py (RunContext/Options: run-state port threaded into every lane)","libs/digest/loader.py (Bundle + load_bundle: read-side port between disk and summarizers)","libs/tools.py (Tools/ToolInfo: external-tool capability port)"],"may_import":["stdlib + sibling pure value modules (context -> paths,tools)"],"must_not_import":["concrete lanes/summarizers/commands/core/pipeline","frameworks/persistence/network types"],"notes":"These narrow data seams ARE the deep-module interfaces: lanes receive RunContext+inv+sym; summarizers receive Bundle; everyone writes through Paths. Keeping them DTO-shaped (not bags of services) lets the validator scan the seams. The agent's default Protocol form is intentionally satisfied by frozen/plain dataclass DTOs here to preserve the proven reference contract."},"infrastructure":{"layer":"infrastructure/adapters","modules":["libs/util.py","libs/tools.py","libs/rgpacks.py","libs/lanes/inventory.py","libs/lanes/symbols.py","libs/lanes/rag.py","libs/lanes/static_graph.py","libs/lanes/queries.py","libs/lanes/contracts.py","libs/lanes/tests.py","libs/lanes/derived.py","libs/digest/ranking.py","libs/digest/planning_symbols.py","libs/digest/planning_graph.py","libs/digest/planning_runtime_surfaces.py","libs/digest/planning_tests.py","libs/digest/planning_gaps.py","libs/digest/planning_digest.py","libs/digest/upload_package.py"],"may_import":["domain values (config, ids, chunker, markdown)","ports/DTOs (paths via ctx, context.RunContext, loader.Bundle, tools)","shared infra (util, tools, rgpacks)","package constants (queries lane -> SCHEMA_VERSION)","stdlib + lazily-probed optional third-party tools (never at top level)"],"must_not_import":["commands, pipeline (lanes must not reach the orchestrator), core","lanes must not import each other except via the shared rgpacks seam; digest summarizers must not import lanes or commands"],"notes":"DEEP modules: narrow build()/assemble() interface, large hidden implementation; isolated so one failure is a warning, not a crash. All external-tool contact is here behind tools.run/detect_all with graceful degradation."},"interface":{"layer":"interface/cli_api","modules":["phase1_decomposition/core/cli.py","phase1_decomposition/__main__.py"],"may_import":["libs.commands.{decompose,condense,digest} (core/cli only)","core.cli.main (__main__ only)","argparse + stdlib"],"must_not_import":["lanes, digest summarizers, pipeline, or domain modules directly (it goes through commands)","anything that triggers work at import"],"notes":"The 'extremely simple core': parse args, route, return exit code. Owns all user-facing stdout/exit semantics; maps argparse errors (exit 2) and the unknown-command path. core is import-isolated from libs."},"composition":{"layer":"composition","modules":["libs/commands/decompose.py","libs/commands/condense.py","libs/commands/digest.py","phase1_decomposition/__main__.py","console_script phase1-decompose"],"may_import":["everything inward it needs to wire (commands import context/tools/paths/pipeline/digest/loader/util)"],"must_not_import":["n/a (composition is the outermost wiring layer)"],"notes":"In this reference, composition is fused into the command use-cases (each run() both decides and wires) plus the __main__/console-script runtime root. This is the deliberate 'simple core / deep libs' shape: the only places allowed to build RunContext/Tools/Bundle and to call detect_all() at runtime."},"tests":{"layer":"tests","modules":["tests/test_phase1.py"],"may_import":["the public CLI (core.cli.main / build_parser), command.run functions, loader.load_bundle, and any libs module under test","stdlib unittest, tempfile, json, sqlite3, hashlib, pathlib"],"must_not_import":["nothing forbidden; tests sit outside the dependency rules but must stay deterministic/isolated"],"notes":"Run via `python3 -m unittest discover -s tests`. Must treat every optional external tool (pytest/semgrep/ast-grep/faiss/model2vec/ctags/grep-ast) as possibly absent and still pass; asserts byte-determinism via per-file sha256 across two output dirs."}},"lifecycle_policy":{"owner":"libs/commands/*.run() and libs/pipeline.run() (the runtime composition roots)","subprocess_resources":"All external-tool invocation goes through libs/tools.run(), a never-raising subprocess.run wrapper with capture_output + timeout; launch failure/timeout returns None (no leaked process). Tool import-probes run in clean child interpreters (libs/tools._module_importable) so a heavy/broken optional import never pollutes or persists in the parent; libs/tools registers an atexit cleanup for any temp probe artifacts it creates.","sqlite_resource":"rag lane (libs/lanes/rag.py) owns the bm25.sqlite FTS5 connection and must open/commit/close it within build() (context-managed or try/finally) so the DB handle is released even on error; the file is written deterministically.","filesystem":"Output directories are created exactly once via Paths.ensure() inside decompose.run before the pipeline starts; every artifact write goes through util.write_text/write_json/write_jsonl (atomic-style write + flush) so partial lanes leave valid files. condense/digest create derived/ and planner-digest/ via os.makedirs(exist_ok=True) inside their command roots.","error_isolation":"pipeline._stage and condense.write_condensates wrap each lane/summarizer in a narrow try/except that converts a failure into a recorded warning (and a placeholder artifact for condensates) so resource setup in one unit never aborts the whole run; the broad except is scoped strictly to lane/summarizer isolation and re-records, never silently swallows.","no_import_time_resources":"No file, socket, subprocess, DB connection, or thread is opened at import time anywhere; all resource acquisition happens inside the named runtime roots and is released before run() returns."}},"decisions":[{"choice":"Map the user's 'simple core / deep libs' shape onto the agent's default layered model rather than re-rooting into domain/application/ports/infrastructure directories.","reason":"The hard architecture constraint and the proven reference both place a thin dispatcher in core/cli.py and ALL work in libs/{lanes,digest,commands}+leaf modules. Re-naming directories to domain//application/ would break the proven import paths, the pyproject packages table, and 31 passing tests. I therefore preserve the reference's physical layout and express the agent's layers as a LOGICAL overlay (interface=core/cli+__main__, application=commands+pipeline, infrastructure=lanes+digest+util+tools+rgpacks, domain/ports=config/ids/chunker/markdown/paths/context/loader.Bundle). Verified by reading every module's imports."},{"choice":"Treat libs/commands/*.run() as fused application use-case + composition roots, and the pipeline as a pure application orchestrator.","reason":"The reference constructs RunContext/Tools/Paths/Bundle ONLY inside command.run() (decompose.run builds Options+RunContext then calls pipeline.run(ctx); condense/digest build Bundle via loader). pipeline.run(ctx) receives a fully-built context and constructs no external resources. This keeps construction at runtime roots and import-time clean, satisfying the composition-root and import-time rules without adding a separate composition/ package the reference does not have."},{"choice":"Express ports as dataclass DTOs (Paths, RunContext/Options, loader.Bundle, Tools) rather than typing.Protocol.","reason":"The agent default prefers Protocol/ABC seams, but the proven reference threads narrow, plain/field-default dataclasses as the seams between layers (ctx into lanes, Bundle into summarizers). These are still named, scannable seams (not generic service bags), so they satisfy the 'narrow interface / deep module / no hidden dependency bag' intent. Changing them to Protocols would alter the ground-truth behavior I am told to preserve; I record this as a deliberate deviation."},{"choice":"Place util.py, tools.py, rgpacks.py in the infrastructure layer even though ids/chunker (domain) call util.","reason":"util provides hashlib/filesystem/JSON I/O and a stderr logger — these are infra concerns. ids/chunker import only util's pure helpers (sha256_text, token_estimate, clip), which are side-effect-free transforms; the file-writing parts of util are used only by lanes/commands. I keep util as one module (matching the reference) and note the domain only touches its pure subset, so the inward-only rule holds in practice. Verified: ids.py imports only config; chunker.py imports config, ids, and util's pure helpers."},{"choice":"Record digest.run -> condense (sibling command import) as an allowed application-to-application edge.","reason":"digest.run reuses condense_cmd.write_condensates so the digest is always consistent with the Step-2 condensates. Both are application use-cases at the same layer; a sibling reuse edge within application does not violate inward-only direction and avoids duplicating the condensate-writing logic. Verified in libs/commands/digest.py line 13."},{"choice":"Designate the rag lane's sqlite (FTS5 BM25) and all subprocess calls as the resource-lifecycle hotspots owned by composition/command roots.","reason":"Per lifecycle rules, resource-owning adapters must release on error. The only stateful resources are the bm25.sqlite connection (rag lane) and external-tool subprocesses (tools.run, never-raising, timeout-bounded). I assign their open/close discipline to the lane build() (context-managed) and the tools wrapper respectively, with pipeline._stage providing error isolation so a half-built resource never aborts the bundle."},{"choice":"Hard-forbid any libs->core import and any import-time construction/optional-heavy-import across all modules.","reason":"The user's constraint ('core must not be imported by libs') and the determinism/graceful-degradation requirements demand it. Verified the reference has zero from..core/from...core edges inside libs and that heavy deps (faiss/model2vec/yaml/grep_ast) are probed in subprocesses or imported lazily, keeping import cheap and absence non-fatal. This is encoded as scannable import_time_policy.forbidden entries for the validator."}],"needs_input":[],"diagnostics":[]}