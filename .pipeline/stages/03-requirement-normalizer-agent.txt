I have enough grounding. The intent artifact is present and rich; the specs confirm behaviors, determinism, graceful degradation, the 32-test suite, and the no-LLM ban. Now I'll produce the requirements JSON, tracing every entry to the intent/spec while keeping out architecture, file layout, runtime, and test-runner choices (those belong to downstream stages).

{"status":"ok","summary":"Normalized intent + grounded facts into observable, testable functional and non-functional requirements for the phase1_decomposition CLI, with stated bans (no LLM, no full test suite, no arbitrary server startup) copied verbatim into forbidden_patterns and quality words made scannable; architecture/runtime/test-runner choices deliberately omitted.","data":{"functional_requirements":["The CLI accepts exactly three subcommands: `decompose`, `condense`, and `digest`; an unknown or missing subcommand is rejected with a non-zero exit.","`decompose` reads a Python repo directory (given as --repo) and writes an artifact bundle to an output directory (given as --out); both arguments are required and their absence is an error.","`decompose` produces a bundle root containing ARTIFACT_GUIDE.md plus the directories inventory/, symbols/, rag/, static/, queries/, contracts/, tests/, and derived/.","inventory/ contains files.jsonl, git-tracked-files.txt, and source-coverage.json.","Each files.jsonl row carries at least: path, language, category, size_bytes, line_count, sha256, git_tracked.","Files are classified into the categories source, test, docs, config, deployment, vendor, and generated.","When the repo is a git repository the tracked-file list is obtained from git ls-files; when it is not a git repo a filesystem-walk fallback produces the file list instead.","symbols/ contains symbols.jsonl, imports.jsonl, occurrences.jsonl, tags, and tags.jsonl.","Each symbols.jsonl row carries at least: symbol_id, name, kind, path, range (with start_line and end_line), and signature.","Symbol ids follow a SCIP-flavored descriptor grammar of the form `python <dotted.module>/<descriptor...>`, where a class is `Name#`, a function/method is `name().`, a module-level term is `name.`, and the module itself is `python <dotted.module>/`.","Module dotted names strip a leading source-root prefix (e.g. `src/`) and collapse `__init__.py` to its package (e.g. `app/api/__init__.py` -> `app.api`).","rag/ contains spans.jsonl, chunks.jsonl, bm25.sqlite, rg-results.jsonl, and (when embeddings run) vectors.faiss plus vector-metadata.json.","Each chunks.jsonl row carries at least: chunk_id, path, range (start_line, end_line), chunk_type, and text.","A chunk_id, span_id, and node_id can be linked back to the symbol they describe so artifacts cross-reference consistently (e.g. a symbol resolves to its span, its chunks, and its graph node).","static/ contains nodes.jsonl and edges.jsonl.","Graph nodes use the node types Repository, File, Module, Class, Function, Method, Import, Dependency, Test, ConfigFile, and DocSection.","Graph edges use the edge types CONTAINS, IMPORTS, CALLS_APPROX, INHERITS, DECORATED_BY, MENTIONS, and TESTS_APPROX.","Approximate edges (such as CALLS_APPROX and TESTS_APPROX) carry confidence/provenance marking them as approximate.","queries/ contains rules/rg/*.json, results/rg.jsonl, and (when the corresponding tool runs) results/semgrep.json, results/semgrep.sarif, results/ast-grep.json, and results/grep-ast/*.md.","Query packs target common Python surfaces: web-framework route decorators (FastAPI/Flask/Django), task/worker decorators (Celery/RQ/Dramatiq), CLI command definitions (Click/Typer/argparse), model/schema patterns (SQLAlchemy/Django/Pydantic), env-var usage, settings/config keys, plugin registries/factories, datastore/session/cache/storage usage, and auth/security surfaces.","The number of digested ripgrep hits captured per query pack is bounded by --rg-cap (default 80).","contracts/ contains openapi.json and contract-sources.md.","Contract discovery is static by default; live OpenAPI extraction that imports/executes repo code happens only when --contracts-import is explicitly passed, and the method used is recorded in contracts/contract-sources.md.","tests/ contains pytest-collect.txt and test-files.jsonl; test collection uses pytest --collect-only (collection only, never execution).","When pytest collection cannot run, test inventory is produced from a static file scan and that fallback is recorded as a warning.","derived/ (after decompose) contains repo-summary.md and artifact-index.md.","ARTIFACT_GUIDE.md states which repo was decomposed, what each directory contains, which tool produced each artifact, artifact counts, warnings/skipped areas, and a recommended reading order; it serves in place of a custom JSON manifest.","`condense` reads an existing bundle (given as --in) and writes the Step-2 planning condensates planning-symbols.md, planning-graph.md, planning-runtime-surfaces.md, planning-tests.md, and planning-gaps.md into the bundle's derived/ directory.","planning-symbols.md includes ranked summaries (symbol counts by kind, top modules/files by symbol count, largest classes, most-decorated symbols, route/worker/CLI/model/config symbols, top and external imports, parse errors) and representative examples with file/line anchors, not every symbol.","planning-graph.md includes node counts by type, edge counts by type, top files/modules by total/incoming/outgoing degree, top import hubs, top approximate call hubs, inheritance roots, decorator clusters, simple path-prefix/import clusters, and preserved warnings about unresolved/dynamic calls.","planning-runtime-surfaces.md summarizes web routes, API path/method info, task/worker, CLI, model/schema, env vars, config keys, datastore/cache, auth/security, plugin/registry, LLM-integration, entrypoint, and test-area signals derived from query results, contracts, and selected symbol/graph summaries.","planning-tests.md includes test file count, test function count, top test directories, tests grouped by likely subsystem, pytest collection status, and a warning when pytest was skipped and only the static scan was used.","planning-gaps.md makes uncertainty explicit, listing skipped tools, parse errors, unresolved call counts, missing vectors when embeddings are skipped, missing OpenAPI when contracts are only derived, dynamic import/dispatch limitations, approximate-only evidence areas, and any warnings emitted during decomposition.","`digest` reads an existing bundle (given as --in) and writes derived/planning-digest.md; when the Step-2 condensates are missing it generates them first.","planning-digest.md includes file/category counts, repo size and coverage, major directories/packages, likely product purpose, main runtime surfaces, likely subsystems, dependency/deployment/config overview, docs summary, large/important areas, weak/uncertain areas, explicit planning warnings, and section-planning considerations, and it surfaces the planning-critical points: top modules by symbol count, top files by graph degree, top import clusters, route/worker/CLI/model/config-env/test-area summaries, most-central functions/classes, and unresolved/dynamic areas.","`digest` can optionally assemble an upload package directory (default location alongside the bundle, overridable with --out) containing README_FOR_PLANNER.md, copies of the five planning condensates, planning-digest.md, and upload-list.md.","README_FOR_PLANNER.md states the repo name/path, digest generation context, the source decomposition path, a warning that the package is a digest rather than raw source, the list of digest files, a recommended reading order, and the Phase-2 output contract (plans/document-plan.json, plans/document-plan.md, plans/section-plans.jsonl).","upload-list.md tells the user exactly which files to upload for planning and explicitly excludes the raw large backend artifacts (symbols.jsonl, static/nodes.jsonl, static/edges.jsonl, rag/chunks.jsonl, rag/spans.jsonl, rag/bm25.sqlite).","The total planner-facing upload stays within a configurable token budget (default 250000) using a conservative estimate of roughly four characters per token; when over budget, representative examples are trimmed first and counts, coverage warnings, and skipped-tool/gap notes are never trimmed.","Raw large artifacts are not duplicated into the digest or upload package; the upload package contains only condensed summaries plus the README and upload list.","Every representative example in the condensates and digest keeps file/line source anchors where they are available.","When an optional external tool is absent, the affected lane writes a clear skip note into its artifact and a corresponding warning into ARTIFACT_GUIDE rather than aborting the run, and the overall command still completes with a success exit code."],"python_runtime_constraints":["No LLM calls anywhere in the decompose, condense, or digest commands.","Implementation favors the standard library; third-party packages are limited to an allow-listed set (the stated core dependencies plus opt-in heavy extras), with external command-line tools used only where they are the industry-practice producer for an artifact.","The tool must run and complete successfully when the heavy/optional external tools (pytest, semgrep, ast-grep, faiss, model2vec, universal-ctags, grep-ast, ripgrep) are not installed.","No network access is required for any command to complete.","Optional heavy capabilities (embeddings/vectors, semgrep, ast-grep, grep-ast) are opt-in and gated behind auto/on/off toggles, defaulting to auto (use only when available)."],"packaging_constraints":["The project ships as an installable package with runnable build metadata.","The package is runnable as `python3 -m phase1_decomposition <decompose|condense|digest>`, which requires a module entry point.","A console-script entry point is exposed for the decompose workflow.","Core dependencies are limited to PyYAML and packaging; heavier capabilities are isolated into opt-in optional extras (an embeddings extra and a grep-ast extra).","Unit tests ship with the package and are runnable via `python3 -m unittest discover -s tests` (not pytest, which is not installed in the target environment)."],"resource_constraints":["Files, subprocesses, and database handles (e.g. the SQLite FTS5 index and any temp files) are released even on error, via context managers or explicit cleanup.","Subprocess invocations of external tools are bounded by timeouts and never raise out of the runner; a launch failure or timeout is treated as tool-absent and degrades gracefully.","No service, client, database, index, or file resource is opened at import time; resources are created only when a command runs.","Decompose must not execute the repo's test suite; it performs collection-only test discovery.","Decompose must not start arbitrary servers or import/execute repo application code by default; executing repo code is confined to the explicit, documented --contracts-import unsafe mode."],"testability_constraints":["Repeated runs of decompose on the same repo produce byte-identical data artifacts, provable by a determinism test that compares artifact bytes across two runs.","Artifact ordering is stable and content hashes are reproducible, so output does not depend on filesystem iteration order, dict ordering, or wall-clock time.","Importing any package module constructs no service, client, index, subprocess, or file resource, provable by an import-safety test.","Each lane's graceful degradation is provable: with an optional tool absent the command still exits successfully and records a skip/warning, exercisable without the real tool installed.","External tools and any environment- or clock-dependent inputs are isolated so tests are deterministic and need no real network, no real installed external tools, and no ambient environment state; tests operate on temp dirs and fixtures.","Cross-artifact id linking is provable: a symbol id resolves to its span, chunk, and graph node, and route/contract ids reconcile, via targeted assertions.","Ranking/top-N selection is deterministic with a defined, tested tie-break so ordering of equal-ranked items is stable.","The planner-facing upload size is assertable against the configured token budget.","The regenerated suite preserves the reference's 32 test functions (covering ids/classify/chunker, import resolution, route detection, artifact existence and JSON validity, determinism, graph labels, command exits, condensate/upload-package assembly, no-raw-artifacts-in-package, and within-budget checks) all passing under unittest."],"forbidden_patterns":["Any LLM/chat-model API call inside decompose, condense, or digest (no model inference, no remote completion call).","Running the full test suite during decompose (pytest collection is collection-only; no test execution).","Starting an arbitrary server or importing/executing repo application code during decompose unless the explicit --contracts-import unsafe flag is set.","Emitting a custom canonical schema as the primary product: no repo-decomposition.json and no custom framework-facts.json as the canonical product (framework facts are query results + native contracts + planner summaries).","Hard-coding or embedding the MOONSHOT_API_KEY (or any secret/credential) into files, prompts, logs, or generated artifacts.","Trimming counts, coverage warnings, or skipped-tool/gap notes to fit the token budget (only representative examples may be trimmed).","Non-deterministic output: dependence on dict/set iteration order, unsorted directory walks, wall-clock timestamps, or random ordering in artifact bytes.","Mutable default argument: =[], ={}, =set(), =dict(), =list(), =defaultdict(...), =Path(...), =datetime.now() in a signature — use None plus in-body init or field(default_factory=...).","Import-time service/client/index/subprocess/file construction or work at module top level.","Hidden dependency lookup: service locator, ambient global container, dynamic registry, globals()/getattr resolution, or implicit singleton for resolving collaborators.","bare except, except Exception: pass, or silently swallowed errors (catch narrow exceptions only where the contract requires).","eval, unsafe exec, unsafe pickle loads, or shell-string command construction (external tools are invoked with argument lists, not shell strings).","print/stdin used for control flow in library/domain code (user I/O belongs to the CLI/interface layer).","Requiring pytest as the test runner for the package's own tests (the environment runs unittest only)."],"preferred_patterns":["Pure deterministic functions for transformations (parsing, classification, ranking, summarization) so output depends only on inputs.","Centralize id construction in one place so symbol/span/chunk/node ids are guaranteed to agree across artifacts.","Use stdlib ast for Python symbol/graph extraction and SQLite FTS5 for BM25-style lexical search.","Invoke external tools through a single safe runner that probes availability, bounds runtime with a timeout, and returns a degraded result instead of raising.","Treat each lane as an independent producer that can be skipped without breaking the others, recording a structured skip/warning.","Emit JSONL with sorted keys and stable row ordering, and derive content hashes via hashlib for reproducibility.","Public functions and methods carry explicit parameter and return type hints.","Use dataclasses for structured records (e.g. tool-availability info, lane results).","condense/digest reuse the same package, paths, JSONL utilities, and id/artifact conventions as decompose rather than re-implementing parsers."],"decisions":["Made the stated bans scannable as exact tokens: 'no LLM calls' -> forbidden LLM/chat-model API call in all three commands; 'no full test suite' -> forbidden pytest execution (collection-only); 'no arbitrary server startup / import by default' -> forbidden repo-code execution outside the explicit --contracts-import mode.","Translated the hard architecture intent ('core extremely simple, everything in libs', DEEP modules) into testability/preferred guidance (narrow public surface, deterministic pure functions, single id authority) without naming files, packages, or module layout, since structure is owned by a later planner.","Translated 'deterministic, byte-reproducible' into concrete scannable constraints: byte-identical artifacts across runs, no dict/set-order or wall-clock dependence, sorted keys and hashlib hashes, and a defined tie-break for ranking.","Translated 'graceful degradation' into both a functional requirement (skip note + ARTIFACT_GUIDE warning, success exit) and a forbidden pattern (bare/swallowed except), since silent failure would violate the explicit warning-recording contract.","Surfaced an implied resource ban (no import-time resource construction; resources closed on error) from the DEEP-module + determinism + repeated-run-determinism intent, recorded as resource and testability constraints.","Recorded the MOONSHOT_API_KEY non-hard-coding rule from the Phase-2 spec section as a forbidden secret-embedding pattern, because the same package houses the planner handoff contract even though Phase 2 LLM calls are out of Phase-1 scope.","Kept the token-budget trimming priority (trim examples, never counts/warnings/gaps) as both a functional requirement and a forbidden pattern so a validator can scan for the prohibited trimming.","Preserved the reference test count as 32 (verified by grepping the reference tests/test_phase1.py) rather than the intent's stated 31, treating the reference suite as ground truth."],"needs_input":[],"assumptions":["Assumed the reference suite's 32 test functions are the authoritative behavioral contract to preserve, superseding the intent summary's '31'; this should be reconciled by the test-planning stage.","Assumed 'allow_listed_deps' means PyYAML + packaging as core with embeddings/grep-ast as opt-in extras, per the spec's recommended dependencies and the reference packaging, without here fixing exact version pins (owned by the runtime/packaging stages).","Assumed ripgrep, like the other external tools, is optional and the query/exact-search lanes degrade gracefully when it is absent, since the environment lists external tools as not guaranteed installed.","Assumed the optional upload package is produced by `digest` (not a separate command), consistent with the three-subcommand CLI and the spec's 'digest can run both steps'.","Assumed token counting uses the spec's ~4 chars/token heuristic rather than a real tokenizer, since no tokenizer dependency is allow-listed."]},"diagnostics":["Intent artifact (stage 01) present and used as the primary source for goal/constraints/test_expectations/forbidden items; raw prompt used only to locate authoritative spec files.","Grounded against PHASE1_DECOMPOSITION_PLAN.md (output shape, lanes, node/edge types, id example, query packs) and PHASE1_STEP2_STEP3_PLANNING_CONDENSATES.md (condensate file list/contents, token budget, acceptance criteria).","Cross-checked the reference implementation: core/cli.py (subcommands and flag defaults: --rg-cap default 80, --budget-tokens default 250000, auto|on|off toggles), libs/ids.py (SCIP-flavored descriptor grammar), libs/tools.py (timeout-bounded never-raising subprocess runner and import probing), and tests/test_phase1.py (32 test functions).","No file/module/class/runtime/test-runner names were placed in requirement lists; structure, version pins, and runner choice are deferred to downstream planner/runtime/test stages."]}